/*
 * Inputs Messages
 *
 * This contains all the text for the Inputs container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Inputs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Inputs container!',
  },
});

/*
 * NewInputForm Messages
 *
 * This contains all the text for the NewInputForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewInputForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewInputForm container!',
  },
});
